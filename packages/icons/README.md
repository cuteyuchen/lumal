# @luma/icons

Luma 独立图标系统，提供图标注册、分组、Iconify/本地 SVG 适配、data URI、图标组件和图标选择器。

```ts
import { LumaIcon, registerIcons } from '@luma/icons'
import '@luma/icons/style.css'
```

详细说明见仓库文档 `docs/icons.md`。
