# @luma/vben-compat

Luma 的 Vben 常用写法兼容层，当前支持 `useVbenForm` 和 `useVbenVxeGrid` 的常用子集。

```ts
import { useVbenForm, useVbenVxeGrid } from '@luma/vben-compat'
```

兼容层用于迁移旧写法，不完整复刻 Vben。详细说明见仓库文档 `docs/vben-compat-api.md` 和 `docs/migration-from-vben.md`。
