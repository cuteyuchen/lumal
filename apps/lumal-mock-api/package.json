{
  "name": "lumal-mock-api",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "nitro build",
    "build:test": "nitro build --preset node-server",
    "dev": "nitro dev --host 127.0.0.1 --port 5320",
    "preview": "node .output/server/index.mjs",
    "start": "node .output/server/index.mjs",
    "test": "vitest run --config vitest.config.ts tests",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@lumal/core": "0.0.2-beta",
    "h3": "1.15.11",
    "nitropack": "^2.13.4"
  },
  "devDependencies": {
    "@types/node": "^24.6.1",
    "typescript": "^6.0.0",
    "vitest": "^4.1.5"
  }
}
